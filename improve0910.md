# QuotaDeck 개선 계획 — 2026-09-10

## 1. 결론과 조사 범위

**트레이에 프로그램이 남아 있어도 주기적으로 갱신되지 않는 직접 원인은 GUI 실행 경로에 주기 조회·업로드 스케줄러가 연결되어 있지 않다는 점이다.** 현재 GUI는 시작 시 미리보기를 한 번 조회하고, 이후에는 사용자 조작에 반응한다. 반복 실행 루프는 별도 CLI 명령인 `quotadeck run`에만 있다.

따라서 Windows가 숨겨진 창의 타이머를 정지시킨다고 추정하기 전에, GUI의 반복 실행 경로부터 구현해야 한다. 타이머를 추가할 때 설정 적용에 따른 런타임 초기화와 무기한 I/O 대기를 함께 해결하지 않으면 갱신 중단이나 과도한 플래시 기록이 다시 발생할 수 있다.

- 조사 기준: 작업 폴더의 소스, 문서, 테스트, 패키징 설정. HEAD는 `e020ec0`이다.
- 이번 산출물은 개선 계획서이며 프로그램 구현 변경은 포함하지 않는다.
- 코드로 확인한 결함, 격리된 실험으로 재현한 결함, 실제 Windows·키보드에서 추가 확인할 사항을 구분했다.
- 현재 트레이에서 실행 중인 EXE의 빌드와 이 소스의 일치 여부, 실제 API 장애, USB 장애, 절전 복귀 현상은 검증하지 않았다. 아래 직접 원인은 **현재 소스 기준**으로 확정한 것이다.

## 2. 프로젝트 구조와 현재 동작

QuotaDeck은 이미 로그인한 Codex·Claude·Cursor·Grok 계정을 탐색하고 사용량을 공통 모델로 변환한 뒤, AULA F108 Pro LCD에 표시하는 Python/PySide6 애플리케이션이다. 렌더링과 장치 통신이 분리되어 있어 하드웨어 없이 회귀 테스트를 확장하기 좋은 구조다.

| 영역 | 주요 파일 | 역할 |
| --- | --- | --- |
| 실행·배포 | `cli.py`, `__main__.py`, `QuotaDeck.spec` | CLI와 GUI 진입점, 콘솔 없는 Windows EXE |
| GUI·트레이 | `app/main_window.py`, `app/tray.py` | 계정 설정, 미리보기, 수동 업로드, 창 숨김 |
| 계정·사용량 | `discovery/`, `providers/`, `http.py` | 로그인 탐색, 공급자별 조회, HTTP·RPC |
| 실행 정책 | `core/scheduler.py`, `core/flashbudget.py` | 조회, 변경 감지, 업로드 간격·일일 한도 |
| 표시 | `core/severity.py`, `renderer/`, `themes/` | 상태 판정, 장면 구성, 이미지 생성 |
| 장치 통신 | `devices/aula_f108/` | RGB565 페이로드, HID 전송, ACK 확인 |
| 설정·자동 실행 | `config.py`, `app/startup.py` | 설정 저장, Windows Run 레지스트리 등록 |

표의 Python 경로는 `src/quotadeck/` 기준이다.

현재 실행 흐름은 다음과 같다.

```text
GUI / EXE
  run_app → MainWindow → refresh_preview (시작 시 1회)
                        └→ apply → 런타임 재생성
                        └→ PreviewWorker → poll → build_frames
  사용자 업로드 클릭 → Worker → tick(force=True)
  창 닫기 → hide; 앱 이벤트 루프와 트레이 유지
  주기적으로 poll 또는 tick을 호출하는 연결 없음

CLI: quotadeck run
  run_forever → tick → poll → maybe_upload → sleep → 반복
```

기본 설정의 의미도 분리해서 유지해야 한다.

| 설정 | 기본값 | 개선 후 보장할 의미 |
| --- | --- | --- |
| `poll_seconds` | 60초 | API 사용량 조회를 예약하는 주기 |
| `scene_hold_seconds` | 10초 | 이미 생성한 LCD 장면의 표시 시간 |
| `min_upload_minutes` | 10분 | 자동 LCD 기록 사이의 최소 간격 |
| `max_age_minutes` | 60분 | 마지막 LCD 기록이 오래되었을 때 재기록을 검토하는 기준 |
| `daily_flash_limit` | 100회 | 동일 장치에 대한 하루 기록 제한 |

API 조회 성공과 LCD 업로드 성공은 별도 사건이다. 값이 같거나 기록 제한에 걸리면 API 조회는 성공해도 LCD 기록은 생략할 수 있다. 최대 경과시간 역시 일일 한도나 장치 장애를 넘어서는 기록 보장이 아니다.

## 3. 확인한 원인과 개선점

### 3.1 P0 — GUI에 반복 실행 경로가 없음

**근거:** `app/main_window.py:250`은 시작 시 `refresh_preview()`를 한 번 호출한다. `refresh_preview()`와 `upload_now()`는 각각 일회성 QThread를 시작한다. `app/preview_widget.py:17`의 QTimer는 기존 프레임을 넘길 뿐 API를 조회하지 않는다. 반복 루프는 `core/scheduler.py:148`에 있으며 `cli.py:185`의 `cmd_run()`에서만 실행한다.

`app/main_window.py:500`의 `closeEvent()`는 창을 숨기고, `run_app()`은 마지막 창이 닫혀도 앱을 종료하지 않도록 설정한다. 이 동작은 트레이 상주를 설명하지만 갱신을 수행하지 않는다. README의 “트레이로 숨긴 뒤에도 조회가 이어집니다”와 구현이 불일치한다.

**개선:** GUI 수명과 함께 유지되는 갱신 컨트롤러를 추가하고 창 표시 여부와 독립적으로 실행한다. GUI 이벤트 루프에서 `run_forever()`를 직접 호출하지 않는다.

### 3.2 P0 — 설정 적용·미리보기마다 실행 이력이 초기화됨

**근거:** `app/main_window.py:376`의 `apply()`가 매번 `QuotaDeckRuntime(self.config)`를 생성한다. 미리보기와 수동 업로드는 모두 먼저 `apply()`를 호출한다.

그 결과 `last_hash`, 이전 스냅샷, 상태 전환 이력, 마지막 기록 시각, 당일 기록 횟수가 사라진다. 현재 수동 업로드를 반복하면 매번 새 예산 객체를 사용하므로 기존 일일 한도가 누적되지 않는다. 자동 타이머를 `refresh_preview()` 또는 `upload_now()`에 단순 연결하면 이 결함도 반복 실행된다. 특히 수동 업로드는 `force=True`이다.

**개선:** 런타임은 유지하고 설정만 갱신한다. 설정 저장, Windows 자동 실행 등록, 사용량 조회를 별도 작업으로 나눈다. 일반적인 자동 조회는 설정 파일이나 레지스트리를 쓰지 않는다. 정책 변경 시 기존 예산과 성공 이력을 보존한다.

### 3.3 P0 — RPC·Windows HID에서 타임아웃이 실제 대기를 제한하지 못함

**RPC 근거:** `providers/codex/appserver_rpc.py:19`의 `_read_line()`은 `timeout` 인자를 사용하지 않고 `stdout.readline()`을 호출한다. `_rpc()`의 deadline 검사는 그 호출이 반환되어야 진행된다. 빈 줄이나 JSON이 아닌 줄을 재귀로 처리하는 부분도 제한시간을 다시 검사하지 않는다.

격리 실험에서 1초 동안 출력 없이 대기하는 Python 자식 프로세스에 `_rpc(..., timeout=0.05)`를 호출했으나 반환까지 약 **1.06초** 걸렸다. 실제 공급자 API나 로그인 파일을 사용하지 않고 타임아웃 결함을 재현했다. 자식이 계속 살아 있으면서 줄을 보내지 않는 상황에서는 무기한 대기할 수 있다.

**HID 근거:** `devices/aula_f108/transport_win32.py:223`의 `read_lcd_ack()`는 `timeout_ms`를 버리고 동기 `ReadFile`을 호출한다. 기본 Windows 전송 선택은 `device.py:74`에서 win32가 우선이다. ACK 미응답 시 장시간 정체할 가능성이 있으나 실제 장치에서 재현하지는 않았다.

**개선:** RPC는 읽기 큐 또는 비동기 파이프와 단조 시계 deadline을 사용하고, 제한시간 초과 시 소유한 자식 프로세스 및 읽기 작업을 정리한다. HID는 읽기뿐 아니라 쓰기·feature 요청까지 취소 가능한 I/O와 시간 제한을 설계한다. Windows 구현 방식은 Overlapped I/O와 취소 API 적용 가능성을 실제 드라이버에서 검증해 확정한다. 단순히 `_busy=False`로 바꾸거나 QThread를 강제 종료하는 방식으로 대체하지 않는다.

### 3.4 P1 — 실제 화면 변경과 업로드 변경 감지가 불일치함

**근거:** `core/scheduler.py:26`의 `render_hash()`는 모든 사용량 창 중 최솟값 하나를 5% 구간으로 변환한다. 반면 `renderer/layout.py:148`은 첫 두 사용량 창을 각각 표시한다.

격리 실험에서 Cursor의 `AUTO=80%, OTHER=20%`를 `AUTO=60%, OTHER=20%`로 바꾸면 **렌더링 픽셀은 달라지지만 해시는 동일**했다. 최솟값이 유지되는 동안 다른 사용량이 크게 변해도 `unchanged`로 판단할 수 있다. 현재 기본 정책에서는 마지막 업로드로부터 60분이 지날 때까지 갱신이 보류될 수 있으며, 장치 장애나 일일 한도가 있으면 더 길어진다.

5% 구간 내 작은 변화 생략은 기존 테스트에도 명시된 정책이므로 그 자체를 결함으로 보지 않는다. 문제는 화면에 표시하는 별도 사용량의 변화까지 놓치는 것이다.

**개선:** 표시 대상 window의 ID·라벨·잔여량 구간, 표시용 reset 값, 순서, 별명, 표시 모드, 프레임 예산, 테마 버전 등 실제 화면 입력에 대응하는 서명을 정의한다. 표시되지 않는 타임스탬프 변화 때문에 매번 기록하지 않도록 한다. 5% 구간 정책을 유지할지 정밀 표시 정책으로 바꿀지는 명시하되, PC 조회와 미리보기는 최신 값을 반영한다.

### 3.5 P1 — 트레이가 조회 상태와 실패를 충분히 보여주지 못함

**근거:** `app/main_window.py:431`의 `_on_done()`에서만 트레이 아이콘과 마지막 업로드 시각을 갱신한다. `_on_preview()`에는 해당 갱신이 없다. 마지막 조회 시각, 다음 조회 시각, 보류 사유는 표시하지 않는다.

상태 선택도 enum 선언 순서에 `min()`을 적용한다. `HEALTHY`와 `CRITICAL`이 함께 있으면 `HEALTHY`가 선택되는 것을 격리 실험으로 확인했다. 또한 `snapshot_severity()`는 스냅샷의 `fetched_at`을 사용하므로, 단순히 시간이 흘렀다는 이유로 오래된 성공 데이터가 자동으로 STALE이 되지는 않는다.

**개선:** 명시적인 상태 우선순위를 정의하고, 공급자별 마지막 성공 시각과 데이터 경과시간을 추적한다. 트레이는 조회 결과를 받을 때마다 갱신한다. LCD 기록 실패가 최신 조회 결과의 표시까지 막지 않도록 한다.

### 3.6 P1 — 장기 실행·오류 복구·종료 관리가 부족함

- `core/scheduler.py:87`은 계정을 순차 조회하며 공급자 밖으로 전파된 예외를 계정별로 격리하지 않는다. 개별 공급자가 일부 예외를 처리하더라도 한 계정의 예상 밖 실패가 이후 계정의 조회를 중단시킬 수 있다.
- `discovery/accounts.py:18`은 현재 탐색에 없는 설정 계정을 결과에서 제외한다. 자격 증명 파일이 일시적으로 사라진 경우 계정이 조용히 빠질 수 있으므로, 설정된 계정의 OFFLINE 상태와 마지막 성공값을 유지할 필요가 있다.
- `app/main_window.py:452`는 실패마다 `QMessageBox.warning()`을 표시한다. 자동 갱신에 그대로 사용하면 반복 팝업이 발생한다.
- worker의 `done`/`failed` 신호에서 busy를 해제하지만 실제 `QThread.finished`와 연결된 정리 절차가 없다. `close_app()`도 실행 중인 작업을 기다리거나 정리하지 않고 종료한다.
- 공급자 탐색, HID 열거, `tasklist` 호출 일부가 UI 스레드에서 실행되고, `tasklist`에는 timeout이 없다.
- `cli.py:23`의 로깅 설정은 CLI run에서만 호출되며 기본 스트림 출력이다. GUI 공통 회전 파일 로그가 없고 EXE는 `console=False`로 패키징된다.

**개선:** 계정별 실패 격리와 마지막 성공 데이터 보존, 자동 작업의 비모달 오류 표시, 작업 종료 시 정리, 단계별 시간 제한, 공통 파일 로그를 도입한다.

## 4. 권장 구현 설계

### 4.1 앱 수명에 묶인 갱신 컨트롤러

`app/refresh_controller.py`를 신설해 QObject 기반 컨트롤러가 하나의 런타임, 예약 타이머, 실행 중인 worker를 소유하도록 한다. MainWindow는 설정 입력과 결과 표시를 담당한다.

```text
앱 시작 / 예약 시각 / 수동 조회 / 절전 복귀
                  ↓
RefreshController → 중복 실행 확인 → 백그라운드 작업
                                      ├─ 계정별 poll
                                      ├─ snapshots_ready → 계정·트레이 갱신
                                      ├─ 미리보기 생성 → UI 전달
                                      └─ maybe_upload(force=False) → 업로드 결과 전달
                  ↑
        finished → 작업 정리·설정 반영·다음 예약
```

동작 계약은 다음과 같이 정한다.

1. 저장된 활성 계정이 있으면 시작 직후 조회하고 일반 업로드 정책을 평가한다. 최초 계정 선택이 완료되지 않은 상태에서는 자동으로 모든 발견 계정을 LCD에 기록하지 않는다.
2. 기본 60초 주기로 조회를 예약한다. `hide()`·최소화·창 복원은 예약 여부에 영향을 주지 않는다. 숨겨진 미리보기의 애니메이션은 별도로 정지시켜도 된다.
3. 자동·수동 조회, 렌더링, HID 쓰기의 중복 실행을 막는다. 실행 중 도착한 자동 요청은 최대 한 건으로 합치고, 지연된 주기를 누적해서 재생하지 않는다. 작업 시간이 주기를 초과하면 지연 상태를 표시한다.
4. 설정 변경은 진행 중인 작업이 끝난 뒤 적용한다. worker는 시작 시 확정한 설정을 사용하고, 다음 작업부터 새 주기를 사용한다. 계정 변경 시 제거된 계정의 표시 상태는 정리하되 장치 기록 예산은 유지한다.
5. 트레이에 **지금 사용량 조회**를 추가한다. 이 작업은 flash 강제 기록 없이 최신 사용량을 읽는다. 기존 수동 업로드만 명시적으로 `force=True`를 사용하고 일일 한도는 유지한다.
6. 자동 경로의 장치 점검은 UI 경고창에 의존하지 않는다. USB 미연결이나 AULA 프로그램 점유 시 조회는 계속하고 LCD 기록만 보류한다.
7. 실행 상태는 `idle / polling / rendering / uploading / backoff / stopping`으로 관리한다. 표시 결과 신호와 스레드 종료 신호를 구분하고 실제 종료 후에 다음 작업을 시작한다.

GUI와 CLI는 동일한 조회·업로드 정책과 구조화된 결과를 사용한다. 기존 `tick()`의 문자열 반환에만 의존하지 않고 조회 성공 계정, 오류 계정, 업로드 여부·보류 코드·다음 허용 시각을 전달하는 결과 모델을 추가한다.

### 4.2 플래시 보호 정책과 상태 보존

- `QuotaDeckRuntime.update_config()` 같은 설정 갱신 경로를 추가한다. 매 조회마다 런타임을 새로 만들지 않는다.
- 설정과 실행 상태를 분리하고 마지막 성공 업로드, 당일 기록 횟수, 날짜, 마지막 서명 등을 `%APPDATA%/QuotaDeck/`의 별도 상태 파일에 원자적으로 저장한다. 자격 증명과 API 응답 원문은 저장하지 않는다.
- GUI 재시작과 CLI 실행도 같은 장치 상태를 사용한다. 단일 GUI 인스턴스 처리와 GUI·CLI 간 장치 쓰기 잠금으로 동시 기록을 막고, 잠금을 획득한 뒤 예산을 다시 확인한다.
- 현재 `maybe_upload()`는 `force=force or aged`를 사용하므로 `min_upload_minutes=120`, `max_age_minutes=60`이면 60분 경과가 최소 간격을 우회할 수 있다. 개선 후 자동 기록은 항상 최소 간격과 일일 한도를 지킨다. 최대 경과시간은 변경 여부를 재평가하는 조건으로만 사용하고 우회 권한으로 사용하지 않는다.
- 성공한 업로드만 마지막 성공 시각·서명을 갱신한다. 다만 전송이 시작된 뒤 결과가 불명확해진 시도는 별도 기록하고 보수적인 재시도 간격을 적용한다. 실패했다고 즉시 기록 예산을 재사용하며 연속 쓰기하지 않는다.
- 일일 한도의 날짜 기준은 현행 UTC를 우선 유지하고 UI에 명시한다. 로컬 날짜로 변경한다면 마이그레이션과 날짜 경계 테스트를 함께 수행한다.

### 4.3 장애·절전 복귀와 작업 종료

- HTTP의 기존 20초 요청 timeout과 별개로, 공급자 fallback 전체와 조회 주기에 대한 소요시간 한도를 정의한다. 한 요청의 timeout이 여러 요청 전체의 완료시간을 보장한다고 가정하지 않는다.
- RPC 무응답·빈 줄·잘못된 JSON을 제한시간 안에 종료하고 자식 프로세스 및 pipe를 회수한다. 종료 요청만 남기고 작업이 계속 살아 있는 상태를 허용하지 않는다.
- HID 페이지 ACK 제한과 전체 전송 한도를 함께 둔다. 쓰기 실패 후 핸들을 닫고 다음 재연결 때 다시 연다. 실제 I/O가 종료되기 전에 새 업로드를 시작하지 않는다.
- 공급자 장애는 계정별 backoff를 적용하고 정상 계정은 계속 조회한다. 429는 사용 가능한 `Retry-After`를 반영하며 같은 오류의 반복 알림은 억제한다.
- 마지막 정상 스냅샷과 이번 시도의 오류를 별도로 저장한다. 오래된 값을 현재 값처럼 보이지 않게 마지막 성공 시각 및 STALE/OFFLINE 표시를 함께 제공한다.
- 절전 복귀는 Windows 전원 이벤트 또는 타이머 공백 감지로 처리한다. 복귀 직후 한 번 재조회하되 누락된 횟수만큼 실행하지 않는다. 네트워크·USB 복구 전에는 backoff하며 최소 기록 간격을 유지한다.
- 종료 시 타이머와 신규 요청을 중단하고, 네트워크·RPC를 취소·정리한 뒤 worker 종료를 확인한다. HID 기록 중에는 작업 상태를 표시하고 정해진 완료·취소 절차를 따른다.

### 4.4 사용자가 확인할 수 있는 상태와 진단 로그

트레이 툴팁과 창에 다음 정보를 구분해서 표시한다.

```text
최근 조회: 14:30:12 (4개 중 3개 성공)
다음 조회: 14:31:12
최근 LCD 기록: 14:20:15
LCD 상태: 최소 기록 간격 대기, 다음 허용 14:30:15
계정 상태: 1개 재로그인 필요
```

- 보류 코드는 `unchanged`, `cooldown`, `daily_limit`, `device_offline`, `device_busy`, `no_accounts` 등으로 구분한다. 정상적인 생략을 갱신 실패로 표시하지 않는다.
- 상태 우선순위는 enum 순서 대신 명시적인 표로 정의한다. 최소한 CRITICAL·EXHAUSTED가 HEALTHY에 가려지지 않고 오류·오래된 데이터 여부도 드러나야 한다.
- “최신” 여부는 현재 시각과 공급자별 마지막 성공 시각으로 계산한다. 잠정 STALE 기준은 `max(3 × poll_seconds, 180초)`로 두고 설정 가능한 정책으로 관리한다.
- GUI·CLI 공통 회전 로그에 시작 버전, 조회 시작·완료·소요시간, 단계, 업로드 결과, 보류 이유, timeout, 절전 복귀, 종료 결과를 남긴다.
- Authorization·쿠키·토큰·응답 원문은 기록하지 않는다. 기존 `core/mask.py`를 활용하되 구조화된 비밀 필드는 애초에 로그에서 제외한다. 트레이에 로그 폴더 열기와 마지막 오류 확인을 제공한다.

## 5. 그 밖의 프로젝트 개선

| 우선순위 | 항목 | 구체적인 개선 |
| --- | --- | --- |
| P1 | TLS 검증 해제 fallback | `http.py:27`의 `_client(False)`를 제거한다. 회사 CA를 신뢰 저장소 또는 명시한 CA 파일로 처리하고 검증 실패 원인을 보여준다. 인증 요청이 인증서 검증 없이 전송되는 경로를 없앤다. |
| P1 | 설정 내구성 | `load_config()`의 잘못된 JSON·타입·enum·범위에 대한 진단과 복구를 추가한다. `save_config()`는 임시 파일 작성 후 교체하고 이전 정상 설정을 보존한다. 비정상 값으로 과도한 조회·기록이 예약되지 않게 한다. |
| P1 | 회귀 테스트 범위 | 현행 UI smoke 테스트는 `live=False`이다. 컨트롤러를 가짜 공급자·시계·장치와 연결해 실제 반복 호출과 숨김 상태를 검증한다. |
| P2 | 설정 반영 일관성 | `toggle_language()`는 다른 UI 설정도 저장하면서 runtime 설정은 갱신하지 않는다. 언어 변경과 실행 설정 저장을 분리하고 화면·저장값·실행값의 차이를 없앤다. |
| P2 | 탐색·렌더링 비용 | 전체 계정 탐색을 매 tick 수행하는 비용을 측정하고 일정 주기 및 사용자 요청으로 분리한다. 로그인 정보는 필요할 때 다시 읽어 갱신된 인증을 사용한다. 테마·스프라이트는 변경 시 무효화하는 캐시를 둔다. |
| P2 | 테마 설정 일관성 | 현재 `build_frames()`는 기본 테마 경로를 사용하지만 서명에는 `config.theme`를 넣는다. 선택한 테마가 렌더링·서명·배포 자산에 일관되게 적용되도록 한다. |
| P2 | 배포 추적 | EXE 버전·빌드 commit을 상태와 로그에 표시한다. CI에서 만든 EXE로 시작·트레이 숨김·반복 조회를 확인하고 README의 동작 설명을 검증 결과에 맞춘다. |

## 6. 구현 순서와 완료 기준

### 단계 1 — 트레이 갱신 복구에 필요한 최소 변경

대상: `app/refresh_controller.py` 신설, `app/main_window.py`, `app/tray.py`, `core/scheduler.py`, `app/i18n.py`.

- 반복 예약, 작업 중복 방지, 숨김과 독립된 실행을 구현한다.
- 설정 저장과 런타임 재생성을 분리한다.
- 조회 결과를 LCD 전송 결과보다 먼저 UI에 전달하고 자동 실패의 모달 팝업을 제거한다.
- 마지막 조회·다음 조회·업로드 보류 이유를 표시하고 공통 진단 로그를 연결한다.

완료 기준: 가짜 공급자가 반환하는 값을 바꾸었을 때 창이 보이거나 숨겨져 있어도 연속 3회 이상 조회 결과가 반영된다. 미리보기·설정 저장 전후에 마지막 업로드 시각과 기록 횟수가 유지된다.

### 단계 2 — 멈춤과 과도한 기록 방지

대상: RPC·HID·HTTP·장치 점검 경로, `core/flashbudget.py`, 실행 상태 저장소, worker 종료 처리.

- 실제 I/O 시간 제한, 작업 정리, 계정별 실패 격리, backoff를 구현한다.
- 최소 간격 우회 문제를 수정하고 예산 영속화·장치 잠금을 추가한다.
- 종료·재실행·절전 복귀·USB 재연결을 처리한다.

완료 기준: 무응답 RPC와 ACK, 한 계정의 예외, 실행 중 종료가 영구 busy나 프로세스 잔류를 남기지 않는다. 재시작·설정 변경·중복 실행으로 기록 한도를 우회하지 못한다. **실장치 자동 기록 배포는 이 단계까지 검증한 뒤 진행한다.**

### 단계 3 — 변경 감지와 상태 표시 정확도

대상: `render_hash()`, 상태 모델, `core/severity.py`, UI·트레이 결과 반영.

- 표시하는 각 사용량 창의 변화가 서명에 반영되도록 한다.
- 여러 계정의 상태 우선순위와 오래된 데이터 표시를 수정한다.
- 업로드 생략·실패 시에도 PC 미리보기는 최신 조회 결과를 유지한다.

완료 기준: AUTO 값만 변하는 사례가 누락되지 않고, HEALTHY 계정과 CRITICAL 계정이 함께 있을 때 경고가 가려지지 않는다.

### 단계 4 — 배포·문서 마무리

- 설정 오류 복구와 TLS 경로를 정리한다.
- 기존 전체 테스트 및 새 회귀 테스트를 Windows CI에 연결한다.
- 새 EXE로 실제 트레이·장치 장기 실행을 확인하고 빌드 식별자를 남긴다.
- README의 60초 조회·10초 장면 유지·10분 최소 기록 및 보류 정책 설명을 실제 동작과 맞춘다.

## 7. 검증 계획

### 7.1 이번 조사에서 수행한 확인

| 확인 | 결과 |
| --- | --- |
| `.\.venv\Scripts\python.exe -m pytest -q` | **38 passed in 11.69s** |
| GUI·CLI 호출 경로와 QTimer 검색 | GUI 반복 조회 연결 없음; 프레임 재생 타이머만 존재 |
| RPC 0.05초 timeout + 1초 무출력 자식 프로세스 | 약 1.06초 후 반환; 제한시간 불이행 재현 |
| AUTO 80→60%, OTHER 20% 고정 | 렌더링 픽셀 변경, 업로드 해시는 동일 |
| HEALTHY·CRITICAL의 현행 트레이 선택식 | HEALTHY 선택 |
| 소스 실행의 기본 테마 경로 | 현재 작업 폴더의 테마가 존재함을 확인 |

테스트 통과는 기존 검사 범위에 대한 결과이며, 트레이 주기 갱신이나 실제 장치의 안정성을 입증하지 않는다. 실험에서 실제 공급자 API 호출이나 LCD 쓰기는 수행하지 않았다.

### 7.2 추가할 자동 회귀 테스트

| 시나리오 | 기대 결과 |
| --- | --- |
| 시작·표시·숨김·복원 | 같은 컨트롤러가 계속 조회하고 복원 후 최신 값을 표시 |
| 느린 작업 중 여러 timer tick·수동 요청 | 동시 runtime 작업과 HID 쓰기가 각각 최대 1개, 요청 무한 누적 없음 |
| poll 주기 변경 | 진행 중 작업 이후 새 주기 적용, 기록 예산 유지 |
| 미리보기·언어 변경·설정 저장 | 업로드 이력 유지, 의도하지 않은 강제 업로드 없음 |
| 조회 성공 후 HID 실패·cooldown·unchanged | 계정·트레이·미리보기는 최신 결과 반영, LCD 상태는 별도 표시 |
| RPC 무출력·개행 없음·잘못된 JSON·EOF | deadline 내 종료 및 자식·읽기 작업 정리 |
| HID ACK 미응답·전송 중 분리 | 제한시간 내 실패 처리, 다음 시도 전 이전 I/O 종료 |
| 한 공급자 예외·429·인증 소실 | 다른 계정 조회 지속, 해당 계정 오류·오래된 값 명시 |
| 표시되는 비최솟값 window만 변경 | 서명 변경, 간격 허용 후 새 화면 업로드 |
| 5% 구간 내 변화·완전히 같은 값 | 문서화한 생략 정책 준수, 조회 성공 시각은 갱신 |
| 최소 간격 120분·최대 경과 60분 | 자동 기록이 60분 시점에 최소 간격을 우회하지 않음 |
| 일일 한도·날짜 변경·재시작·동시 GUI/CLI | 한도·이력 보존, UTC 날짜 경계에서만 당일 횟수 갱신 |
| 계정 전체 비활성·탐색에서 일시 소실 | 빈 계정 정책 일관, 실패 계정이 정상처럼 사라지지 않음 |
| 절전 복귀·시스템 시각 변경 | 밀린 작업 일괄 실행 없음, 새 조회 1회 예약 및 예산 유지 |
| 작업 중 앱 종료 | 신규 요청 중단, worker·소유 프로세스·핸들 회수 |
| 오류 로그·툴팁 | 원인 파악 가능, 인증정보와 응답 원문 없음 |

가짜 시계와 주입 가능한 공급자·장치로 시간을 가속해 검증한다. 실행 호스트의 로그인 파일, Windows 자동 실행 등록, 실제 키보드 기록에 의존하지 않도록 테스트를 격리한다.

### 7.3 Windows EXE·실장치 인수 테스트

1. 실행 중인 EXE의 경로·버전·빌드 commit을 확인하고 검증 대상 소스와 맞춘다.
2. 기본 60초 조회 상태에서 창을 닫아 트레이로 숨긴 뒤 최소 30분 관찰한다. 정상 네트워크·유휴 worker 조건에서 예약 시각 대비 조회 시작 지연 목표는 5초 이내로 두고 로그로 확인한다.
3. 사용량이 바뀌는 동안 조회 시각·PC 표시가 계속 바뀌는지 확인한다. LCD는 변경 감지와 최소 10분 간격에 따라 갱신되는지 확인한다. 관찰 중 값이 바뀌지 않으면 로그의 `unchanged`를 확인한다.
4. 절전·복귀, 네트워크 끊김·복원, USB 분리·재연결, AULA 프로그램 실행·종료를 각각 수행하고 자동 복구를 확인한다.
5. 8시간 이상 상주하며 영구 busy, 반복 팝업, 중복 프로세스, worker·핸들 누적, CPU·메모리의 지속 증가 여부를 점검한다.
6. 재시작 후 기록 예산이 유지되는지 확인한다. 일일 한도와 긴 시간 경계 검증은 가짜 장치에서 수행해 실장치에 불필요한 반복 기록을 하지 않는다.

최종 완료 조건은 **“트레이가 보인다”가 아니라 “조회 성공 시각이 계속 전진하고, LCD 갱신 또는 보류 이유를 확인할 수 있으며, 장애 후 스스로 복구한다”**이다.
